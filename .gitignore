# 依赖与构建产物（rules-injector 刻意零依赖：无 package.json / node_modules；
# node_modules/、dist/ 仅为对齐姊妹插件 dsh-hanako 的通用清单，防未来引入构建栈误提交）
node_modules/
dist/
releases/

# 临时文件（打包原子写临时文件 .*.tmp、本地验证产物等）
_tmp/

# 系统杂项
*.log
.DS_Store
